/.phpunit.cache
/node_modules
/public/hot
/public/storage
/public/deploy.config.php

# public/build is committed on purpose: the deploy target (shared hosting) has no
# Node, so it serves the pre-compiled assets. Rebuild locally (npm run build) and
# commit when frontend code changes.
!/public/build
/public/build/.vite
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
database/.DS_Store
.DS_Store

/docs
/landing

/WhatsMe
/release
/Chat-Agent-App

# Local archives / dumps — never publish
*.zip
*.tar.gz
*.rar
*.7z
/whatsapp.zip
